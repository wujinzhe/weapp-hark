// 初始化数据

// 创建克隆对象（新对象）
// const draft = getCloneByName('x').createClone('y', data => {
//   return {
//     store: data.store
//   }
// })

// const draft = store.createClone('y')

// // 获取克隆对象
// const clone = getCloneByName('y')


// // 将克隆的对象中的某个值赋值到页面、组件的data数据中
// setStore(this, {
//   store: draft.store,
//   a: draft.store1
// })


// // 更新某个对象
// updateStore(store, {
//   store: this.data.store,
//   store1: this.data.a
// })

// updateStore(getCloneByName('x'), {
//   store: this.data.store,
//   store1: this.data.a
// })

// 初始化数据

// 创建克隆对象（新对象）
// const draft = getCloneByName('x').createClone('y', data => {
//   return {
//     store: data.store
//   }
// })

// const draft = store.createClone('y')

// // 获取克隆对象
// const clone = getCloneByName('y')


// 将克隆的对象中的数据赋值到page,app,component中的data
// setStore(this, draft)
/**
 * this.setData
 */


// // 更新某个对象
// updateStore(store, {
//   store: this.data.store,
//   store1: this.data.a
// })

// updateStore(getCloneByName('x'), {
//   store: this.data.store,
//   store1: this.data.a
// })